import { Open_Sans } from "next/font/google";

const OpenSans = Open_Sans({ weight: "700", subsets: ["latin"] });
const ThinOpenSans = Open_Sans({ weight: "variable", subsets: ["latin"] });

export default OpenSans;

export { ThinOpenSans };
